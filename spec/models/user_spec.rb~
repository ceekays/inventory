require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe User do
  before(:each) do
    
  end

  it "should exist" do
    User.exists?
  end
  it "should have valid attributes" do
    u = User.new
    user = {username => 'villa',first_name=> 'Jonathan', }
    u = 
  end
end

